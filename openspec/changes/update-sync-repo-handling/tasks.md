## 1. Implementation
- [x] 1.1 Update `src/cli/commands/sync.rs` to print "Repository 'name' already exists" if `helm repo add` indicates so.
- [x] 1.2 Verify behavior with manual test (or unit test if mockable).
