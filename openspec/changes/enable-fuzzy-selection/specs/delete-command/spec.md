## MODIFIED Requirements

### Requirement: Chart Selection
The `delete` command MUST allow users to select a chart from a list if no name is provided or ambiguity exists.

#### Scenario: Filtering
Given the chart selection list is displayed
The user MUST be able to type text to filter the list of available charts (fuzzy search).
