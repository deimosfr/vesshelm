# Tasks: Enable Helm Diff Colors

- [x] Update `deploy.rs` to set `HELM_DIFF_COLOR=true` environment variable when creating the `helm diff` command.
- [x] Verify `helm diff` output contains ANSI color codes (or just visually verify).
