# Spec: cli-graph-command

## ADDED Requirements

### Requirement: Graph Command
The system MUST provide a command to view the deployment dependency graph.

#### Scenario: View Graph
- **WHEN** User runs `vesshelm graph`.
- **THEN** System displays charts in the resolved deployment order.
